# Local development settings. Copy to .env to override the defaults.
# These are throwaway credentials for the local MinIO container only.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_BUCKET=nfl-data
